# Build on JDK 21, and use the Maven Wrapper so JitPack builds with a modern
# Maven (its default is too old for maven-compiler-plugin 3.13).
jdk:
  - openjdk21
install:
  - ./mvnw install -DskipTests
